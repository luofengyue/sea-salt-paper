page {
  min-height: 100vh;
  background: #f2fbff;
  color: #24485f;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

button {
  padding: 0;
  margin: 0;
  border-radius: 0;
  font-family: inherit;
}

button::after {
  border: none;
}
